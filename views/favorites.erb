<h1 class="fav-h1"> <%= current_user.name %>'s Favourites</h1>

<% @likes.each do |like| %>

	<ul class="fav-ul">
		<li class="fav-li"> <%= like.recipe_name %> </li>
		<li class="fav-li"> <img class="fav-image" src="<%= like.image_url %>"> </li>
		<li class="fav-li"> <a href="<%= like.recipe_url %>">Go to Recipe </a> </li>
	</ul>

<% end %>
